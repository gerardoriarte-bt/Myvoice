# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.*

# Server ignores
server/.env
server/node_modules
server/dist

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
Myvoice.pem

# PDFs de guías de marca subidos por usuarios — datos, no código
server/uploads/

# Configuración local del asistente
.claude/

# Scratch de agentes de auditoría
server/.oraculo/

# Reportes de corridas de backfill: evidencia de una base concreta, no código
server/.backfills/

# Archivo de diseño de Pencil: binario cifrado que crece con cada versión y que
# git no puede difear ni fusionar. Vive en local; lo que el repo versiona es la
# documentación de diseño en docs/diseno-sistema.md.
design/

# El video de comunicación queda FUERA del repo, completo: el proyecto de
# Remotion, las capturas que usa y el mp4 renderizado. No es del producto, y
# vive en la máquina de quien lo edita.
video/
